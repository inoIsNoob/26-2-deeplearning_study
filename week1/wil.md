relu 함수를 구현할 때 if문으로 분기를 만들기보다 max()함수를 사용하는 것이 더 효율적임을 깨달았다.
perceptron 함수를 구현할때 가중치 b를 마지막에 1번만 더하고 relu함수를 한번 통과시켜서 return한다는 것을 깨달았다.
딥러닝의 머신러닝 분야중에 하나라는 것을 배웠다
딥러닝을 할때 중요한 함수들(relu, perception, softmax, sigmoid)을 배웠다.
첫주차라 세팅을 하는것에 조금 어려움을 겪었지만 세팅영상에 설명이 상세히 되어있어서 Github에 연동까지 성공했다.
fashion_mnist는 28x28의 픽셀을 가진다는 것을 알게되었다
fit()함수는 입력 데이터와 정답 데이터를 모델에 입력하여 모델의 가중치를 업데이트 한다는 것을 알게되었다